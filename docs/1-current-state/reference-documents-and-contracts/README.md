# Reference Documents And Contracts

Stable CSV/file contracts and reference documents belong here when they are not a normal workflow, CSV/file contract, storage model, S3/MinIO integration, or runtime source-of-truth document.

If work is needed to become compliant with a contract, track that work in `../active-tasks.md` and `../current-progress.md`.
